import { ptBR } from "./pt-BR";
import { defaultLocale } from "./config";
import type { Messages } from "./types";

export const locale: string = defaultLocale;
export const messages: Messages = ptBR;

export type { Messages };
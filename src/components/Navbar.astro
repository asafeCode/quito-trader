---
import { messages } from "../i18n";
import CTAButton from "./CTAButton.astro";

const t = messages.nav;
---

<header
  class="fixed inset-x-0 top-0 z-50 border-b border-line/40 bg-bg/60 backdrop-blur-xl"
>
  <nav
    class="container-page flex h-16 items-center justify-between gap-6"
    aria-label={t.accessibilityNav}
  >
    <a
      href="#top"
      class="flex items-center gap-3"
      aria-label={`${messages.site.name} — ${t.accessibilityHome}`}
    >
      <img
        src="/logo.png"
        alt={messages.site.name}
        width="40"
        height="40"
        class="h-9 w-9 object-contain"
        loading="eager"
      />
      <span class="hidden flex-col leading-tight sm:flex">
        <span class="font-display text-sm font-bold tracking-tighter text-ink">
          QUITOTRADER
        </span>
        <span class="text-[10px] uppercase tracking-[0.22em] text-muted">
          {messages.site.tagline}
        </span>
      </span>
    </a>

    <ul class="hidden items-center gap-8 md:flex">
      <li>
        <a
          href="#sobre"
          class="text-sm text-muted transition-colors hover:text-ink"
        >
          {t.sobre}
        </a>
      </li>
      <li>
        <a
          href="#metodo"
          class="text-sm text-muted transition-colors hover:text-ink"
        >
          {t.metodo}
        </a>
      </li>
      <li>
        <a
          href="#resultados"
          class="text-sm text-muted transition-colors hover:text-ink"
        >
          {t.resultados}
        </a>
      </li>
    </ul>

    <div class="flex items-center gap-3">
      <a
        href={messages.links.stockity}
        target="_blank"
        rel="noopener noreferrer"
        class="hidden items-center gap-2 rounded-full border border-line bg-surface/80 px-3 py-1.5 text-xs text-muted transition-colors hover:border-accent/60 hover:text-ink lg:flex"
        aria-label="Stockity"
      >
        <img
          src="/stockity-logo.png"
          alt={messages.accessibility.altStockity}
          class="h-5 w-auto object-contain"
          loading="lazy"
          width="80"
        />
        <span class="font-medium">{t.parceria}</span>
      </a>
      <CTAButton short />
    </div>
  </nav>
</header>